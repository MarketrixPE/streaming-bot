apiVersion: 1

providers:
  - name: streaming-bot
    folder: Streaming-Bot
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
